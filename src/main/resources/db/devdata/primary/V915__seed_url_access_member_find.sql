-- ============================================================================
-- V915 — 아이디 찾기 URL 접근 규칙 (P10-2)
-- ----------------------------------------------------------------------------
-- 로그인하지 못하는 사람이 쓰는 화면이라 회원 영역 잠금(55·60)보다 앞서 열려야 한다.
-- V914(가입, 53) 바로 뒤인 54 에 둔다.
--
-- ※ V914 에 이어 붙이지 않고 새 파일로 만든 이유: V914 는 이미 적용된 마이그레이션이라
--   내용을 바꾸면 checksum 이 어긋나 다음 기동이 실패한다(flyway-migration.md 규약).
--
-- 남용(이메일 대입으로 가입 여부 확인)은 URL 규칙이 아니라 화면·응답 형태로 막는다 —
-- 찾았을 때와 못 찾았을 때가 같은 화면으로 돌아온다.
-- ============================================================================

INSERT INTO tb_role_url_access
  (url_access_id, site_id, site_code, url_pattern, http_method, access_type,
   required_roles, allowed_user_types, priority, description) VALUES

('RUA_01985a10-0000-7000-8000-000000001994', NULL, NULL, '/*/member/find-id', 'ALL',
 'PERMIT_ALL', NULL, NULL, 54,
 '아이디 찾기 — 로그인 못 하는 사람이 쓰는 화면이라 열려 있어야 한다')
;
