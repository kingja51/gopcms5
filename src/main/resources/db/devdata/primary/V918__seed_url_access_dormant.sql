-- ============================================================================
-- V918 — 휴면 복원 URL 접근 규칙 (P10-5)
-- ----------------------------------------------------------------------------
-- 휴면 계정은 로그인이 되지 않는 상태라 <b>비로그인</b>으로 이 화면에 온다.
-- 회원 영역 잠금(55·60)보다 앞서 열려야 한다 — 아이디 찾기(54)와 같은 성격이다.
--
-- 남용 방지는 URL 규칙이 아니라 안쪽에서 한다:
--   · 아이디+비밀번호가 모두 맞아야 휴면으로 인정(계정 존재 노출 차단)
--   · 인증번호는 해시로만 보관, 시도 5회 제한(행에 저장), 재발송 쿨다운
-- ============================================================================

INSERT INTO tb_role_url_access
  (url_access_id, site_id, site_code, url_pattern, http_method, access_type,
   required_roles, allowed_user_types, priority, description) VALUES

('RUA_01985a10-0000-7000-8000-000000001998', NULL, NULL, '/*/member/dormant', 'ALL',
 'PERMIT_ALL', NULL, NULL, 50,
 '휴면 복원 — 로그인할 수 없는 상태에서 쓰는 화면'),

('RUA_01985a10-0000-7000-8000-000000001999', NULL, NULL, '/*/member/dormant/**', 'ALL',
 'PERMIT_ALL', NULL, NULL, 50,
 '휴면 복원 하위 — 본인확인·인증번호 검증')
;
