-- ============================================================================
-- V916 — 비밀번호 찾기 URL 접근 규칙 (P10-2)
-- ----------------------------------------------------------------------------
-- 아이디 찾기(V915, 54)와 같은 성격 — 로그인하지 못하는 사람이 쓰는 화면이라
-- 회원 영역 잠금(55·60)보다 앞서 열려야 한다.
--
-- 남용(아이디·이메일 대입으로 가입 여부 확인)은 응답 형태로 막는다 —
-- 계정이 있든 없든 같은 안내로 끝난다. 발송 자체의 남용은 로그인 레이트리밋과
-- 별개로 P10 후속에서 요청 빈도 제한을 검토한다.
-- ============================================================================

INSERT INTO tb_role_url_access
  (url_access_id, site_id, site_code, url_pattern, http_method, access_type,
   required_roles, allowed_user_types, priority, description) VALUES

('RUA_01985a10-0000-7000-8000-000000001995', NULL, NULL, '/*/member/find-password', 'ALL',
 'PERMIT_ALL', NULL, NULL, 54,
 '비밀번호 찾기 — 로그인 못 하는 사람이 쓰는 화면이라 열려 있어야 한다')
;
