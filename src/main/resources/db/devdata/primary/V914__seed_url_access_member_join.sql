-- ============================================================================
-- V914 — 회원 가입 URL 접근 규칙 (P10-1)
-- ----------------------------------------------------------------------------
-- 회원 영역(`/*/member/**`)은 P6-2 에서 AUTHENTICATED 로 잠겨 있다(priority 60).
-- 가입은 <b>로그인하기 전에</b> 하는 일이라 그 앞에서 열어 줘야 한다.
--
-- priority 는 53 — 사이트 스코프 회원 규칙(55)·전역 회원 규칙(60)보다 앞이다.
-- 뒤에 두면 가입 화면이 로그인을 요구하고, 로그인하려면 가입해야 하는 상태가 된다.
--
-- 가입 자체의 남용(자동 가입 등)은 URL 규칙이 아니라 안쪽에서 막는다 —
-- CAPTCHA·레이트리밋은 P10-2 에서 붙인다.
-- ============================================================================

INSERT INTO tb_role_url_access
  (url_access_id, site_id, site_code, url_pattern, http_method, access_type,
   required_roles, allowed_user_types, priority, description) VALUES

('RUA_01985a10-0000-7000-8000-000000001993', NULL, NULL, '/*/member/join', 'ALL',
 'PERMIT_ALL', NULL, NULL, 53,
 '회원 가입 — 비로그인 전용 경로(회원 영역 잠금보다 앞서 열린다)')
;
