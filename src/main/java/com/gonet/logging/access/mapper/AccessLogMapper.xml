<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gonet.logging.access.mapper.AccessLogMapper">

    <!-- logged_at/created_at 은 DB DEFAULT current_timestamp().
         created_ip = client_ip (적재 주체가 곧 요청자 — 감사 3종 규약 충족) -->
    <insert id="insert" parameterType="com.gonet.logging.access.dto.AccessLog">
        INSERT INTO log_access (
            site_id, site_code, menu_id,
            actor_user_id, actor_user_type, actor_login_id,
            request_uri, http_method, query_string, referer,
            client_ip, user_agent, status_code, response_ms, bytes_sent,
            session_id, trace_id, created_ip
        ) VALUES (
            #{siteId}, #{siteCode}, #{menuId},
            #{actorUserId}, #{actorUserType}, #{actorLoginId},
            #{requestUri}, #{httpMethod}, #{queryString}, #{referer},
            #{clientIp}, #{userAgent}, #{statusCode}, #{responseMs}, #{bytesSent},
            #{sessionId}, #{traceId}, #{clientIp}
        )
    </insert>

</mapper>
