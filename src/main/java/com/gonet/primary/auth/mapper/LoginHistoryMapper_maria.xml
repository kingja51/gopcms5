<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gonet.primary.auth.mapper.LoginHistoryMapper">

    <insert id="insert" parameterType="com.gonet.primary.auth.dto.LoginHistory">
        INSERT INTO tb_login_history
            (login_history_id, user_type, user_id, login_id, site_id, site_code,
             result, fail_reason, client_ip, user_agent, session_id, created_ip)
        VALUES
            (#{loginHistoryId}, #{userType}, #{userId}, #{loginId}, #{siteId}, #{siteCode},
             #{result}, #{failReason}, #{clientIp}, #{userAgent}, #{sessionId}, #{clientIp})
    </insert>

    <!--
      직전 성공 로그인 — 이번 로그인 이력이 이미 들어간 뒤라 최신 1건을 건너뛴다(OFFSET 1).
      단순히 "최신 1건" 을 집으면 방금 한 내 로그인이 나와서 아무 의미가 없다.
    -->
    <select id="findPreviousSuccess" resultType="com.gonet.primary.auth.dto.LoginHistory">
        SELECT login_history_id, user_type, user_id, login_id, site_id, site_code,
               result, client_ip, user_agent, attempted_at
        FROM tb_login_history
        WHERE user_id = #{userId}
          AND result = 'SUCCESS'
        ORDER BY attempted_at DESC, login_history_id DESC
        LIMIT 1 OFFSET 1
    </select>

</mapper>
