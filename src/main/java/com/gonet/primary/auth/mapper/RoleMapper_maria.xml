<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gonet.primary.auth.mapper.RoleMapper">

    <select id="findAllRoles" resultType="com.gonet.primary.auth.dto.RoleNode">
        SELECT role_id, parent_role_id, role_code
        FROM tb_role
        WHERE use_yn = 'Y'
          AND delete_yn = 'N'
        ORDER BY sort_order ASC, role_id ASC
    </select>

    <!-- 재전개는 전량 교체 — 부분 갱신은 고아 행을 남긴다 (한 트랜잭션 안에서만 유효) -->
    <delete id="deleteAllHierarchy">
        DELETE FROM tb_role_hierarchy
    </delete>

    <insert id="insertHierarchy">
        INSERT INTO tb_role_hierarchy
            (role_hierarchy_id, ancestor_role_id, descendant_role_id, depth)
        VALUES
        <foreach item="edge" collection="edges" separator=",">
            (#{edge.roleHierarchyId}, #{edge.ancestorRoleId}, #{edge.descendantRoleId}, #{edge.depth})
        </foreach>
    </insert>

    <select id="findAdminRolePairs" resultType="com.gonet.primary.auth.dto.UserRoleRow">
        SELECT ar.admin_id AS user_id, ar.role_id AS role_id
        FROM tb_admin_role ar
        JOIN tb_admin a ON a.admin_id = ar.admin_id AND a.delete_yn = 'N'
        JOIN tb_role r ON r.role_id = ar.role_id AND r.use_yn = 'Y' AND r.delete_yn = 'N'
        WHERE ar.delete_yn = 'N'
        ORDER BY ar.admin_id ASC, r.sort_order ASC
    </select>

    <select id="findAdminRoleCsv" resultType="com.gonet.primary.auth.dto.UserRoleRow">
        SELECT admin_id AS user_id, role_ids AS role_ids, role_codes AS role_codes
        FROM tb_admin
        WHERE delete_yn = 'N'
    </select>

    <update id="updateAdminRoleCsv">
        UPDATE tb_admin
        SET role_ids = #{roleIds},
            role_codes = #{roleCodes},
            updated_by = #{updatedBy}, updated_ip = #{updatedIp}, updated_at = CURRENT_TIMESTAMP
        WHERE admin_id = #{userId}
    </update>

    <select id="findMemberRoleCsv" resultType="com.gonet.primary.auth.dto.UserRoleRow">
        SELECT member_id AS user_id, role_ids AS role_ids
        FROM tb_member
        WHERE delete_yn = 'N'
          AND role_ids IS NOT NULL
          AND role_ids &lt;&gt; ''
        ORDER BY member_id ASC
    </select>

    <!-- ══ 관리 CRUD (P7-3) ═══════════════════════════════════════════════ -->
    <sql id="admWhere">
        WHERE r.delete_yn = 'N'
        <if test="keyword != null">
            AND (r.role_code LIKE CONCAT('%', #{keyword}, '%')
              OR r.role_name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </sql>

    <select id="findPage" resultType="com.gonet.primary.auth.dto.RoleAdmDto">
        SELECT r.role_id, r.site_id, r.site_code, r.parent_role_id, r.role_code,
               r.role_name, r.description, r.sort_order, r.use_yn,
               p.role_name AS parent_role_name,
               (SELECT COUNT(*) FROM tb_role_hierarchy h
                 WHERE h.ancestor_role_id = r.role_id AND h.depth &gt; 0) AS descendant_count,
               (SELECT COUNT(*) FROM tb_admin_role ar
                 WHERE ar.role_id = r.role_id AND ar.delete_yn = 'N') AS admin_count
        FROM tb_role r
        LEFT JOIN tb_role p ON p.role_id = r.parent_role_id
        <include refid="admWhere"/>
        ORDER BY r.sort_order, r.role_code
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countPage" resultType="int">
        SELECT COUNT(*) FROM tb_role r
        <include refid="admWhere"/>
    </select>

    <select id="findAdmById" resultType="com.gonet.primary.auth.dto.RoleAdmDto">
        SELECT role_id, site_id, site_code, parent_role_id, role_code, role_name,
               description, sort_order, use_yn
        FROM tb_role
        WHERE role_id = #{roleId} AND delete_yn = 'N'
    </select>

    <select id="countByCode" resultType="int">
        SELECT COUNT(*) FROM tb_role
        WHERE role_code = #{roleCode} AND delete_yn = 'N'
        <if test="excludeId != null">AND role_id &lt;&gt; #{excludeId}</if>
    </select>

    <!-- 관리자 매핑 + 하위 역할 + URL 규칙에서의 참조를 모두 센다 -->
    <select id="countReferences" resultType="int">
        SELECT (SELECT COUNT(*) FROM tb_admin_role ar
                 WHERE ar.role_id = #{roleId} AND ar.delete_yn = 'N')
             + (SELECT COUNT(*) FROM tb_role c
                 WHERE c.parent_role_id = #{roleId} AND c.delete_yn = 'N')
             + (SELECT COUNT(*) FROM tb_role_url_access u
                 WHERE u.delete_yn = 'N'
                   AND FIND_IN_SET(#{roleId}, REPLACE(IFNULL(u.required_roles, ''), ' ', '')) &gt; 0)
    </select>

    <insert id="insert" parameterType="com.gonet.primary.auth.dto.RoleAdmDto">
        INSERT INTO tb_role
            (role_id, site_id, site_code, parent_role_id, role_code, role_name,
             description, sort_order, use_yn)
        VALUES
            (#{roleId}, #{siteId}, #{siteCode}, #{parentRoleId}, #{roleCode}, #{roleName},
             #{description}, #{sortOrder}, #{useYn})
    </insert>

    <update id="update" parameterType="com.gonet.primary.auth.dto.RoleAdmDto">
        UPDATE tb_role
        SET
            <if test="roleCode != null and roleCode != ''">role_code = #{roleCode},</if>
            <if test="roleName != null and roleName != ''">role_name = #{roleName},</if>
            <if test="description != null and description != ''">description = #{description},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="useYn != null and useYn != ''">use_yn = #{useYn},</if>
            <!--
              site_id·parent_role_id 는 무조건 세팅 — 사이트 전용 역할을 전역으로 돌리거나
              최상위(단독) 역할로 올리려면 NULL 로 <b>비울 수 있어야</b> 한다.
              parent 를 못 비우면 ROLE_PRIVACY 같은 단독 역할을 만들 수 없다.
            -->
            site_id = #{siteId}, site_code = #{siteCode},
            parent_role_id = #{parentRoleId},
            updated_by = #{updatedBy}, updated_ip = #{updatedIp}, updated_at = CURRENT_TIMESTAMP
        WHERE role_id = #{roleId}
    </update>

    <update id="softDelete">
        UPDATE tb_role
        SET delete_yn = 'Y', use_yn = 'N',
            updated_by = #{updatedBy}, updated_ip = #{updatedIp}, updated_at = CURRENT_TIMESTAMP
        WHERE role_id = #{roleId}
    </update>

    <select id="findAllForSelect" resultType="com.gonet.primary.auth.dto.RoleAdmDto">
        SELECT role_id, role_code, role_name, parent_role_id, sort_order
        FROM tb_role
        WHERE use_yn = 'Y' AND delete_yn = 'N'
        ORDER BY sort_order, role_code
    </select>

    <update id="updateMemberRoleIds">
        UPDATE tb_member
        SET role_ids = #{roleIds},
            updated_by = #{updatedBy}, updated_ip = #{updatedIp}, updated_at = CURRENT_TIMESTAMP
        WHERE member_id = #{userId}
    </update>

</mapper>
