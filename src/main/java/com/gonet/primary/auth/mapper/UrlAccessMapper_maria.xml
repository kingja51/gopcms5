<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gonet.primary.auth.mapper.UrlAccessMapper">

    <!--
      평가 순서를 SQL 이 확정한다 (매니저는 첫 매칭을 그대로 채택):
        ① priority ASC          — 운영자가 지정한 우선순위
        ② site_id NOT NULL 우선 — 사이트 규칙이 전역 규칙을 덮는다
        ③ 긴 패턴 우선          — /adm/login 이 /adm/** 보다 구체적
      (site_id IS NULL) · CHAR_LENGTH 는 MariaDB·PostgreSQL 공통 — 벤더 분기 불필요.
    -->
    <select id="findActiveRules" resultType="com.gonet.primary.auth.dto.UrlAccessRule">
        SELECT url_access_id, site_id, site_code, url_pattern, http_method, access_type,
               required_roles, required_auths, allowed_user_types, allowed_ips,
               require_csrf_yn, require_2fa_yn, priority, description
        FROM tb_role_url_access
        WHERE use_yn = 'Y'
          AND delete_yn = 'N'
        ORDER BY priority ASC,
                 (site_id IS NULL) ASC,
                 CHAR_LENGTH(url_pattern) DESC,
                 url_access_id ASC
    </select>

    <!-- ══ 관리 CRUD (P7-3) ═══════════════════════════════════════════════
         목록은 use_yn 무관(중지 규칙도 관리 대상)이고, 정렬은 인가 평가 순서와 같게 둔다 —
         화면에서 보는 순서가 곧 판정 순서여야 오해가 없다. -->
    <sql id="admWhere">
        WHERE r.delete_yn = 'N'
        <if test="keyword != null">
            AND (r.url_pattern  LIKE CONCAT('%', #{keyword}, '%')
              OR r.description  LIKE CONCAT('%', #{keyword}, '%')
              OR r.access_type  LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </sql>

    <select id="findPage" resultType="com.gonet.primary.auth.dto.UrlAccessRule">
        SELECT r.url_access_id, r.site_id, r.site_code, r.url_pattern, r.http_method,
               r.access_type, r.required_roles, r.required_auths, r.allowed_user_types,
               r.allowed_ips, r.require_csrf_yn, r.require_2fa_yn, r.priority,
               r.description, r.use_yn
        FROM tb_role_url_access r
        <include refid="admWhere"/>
        ORDER BY r.priority ASC, (r.site_id IS NULL) ASC,
                 CHAR_LENGTH(r.url_pattern) DESC, r.url_access_id ASC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countPage" resultType="int">
        SELECT COUNT(*) FROM tb_role_url_access r
        <include refid="admWhere"/>
    </select>

    <select id="findById" resultType="com.gonet.primary.auth.dto.UrlAccessRule">
        SELECT url_access_id, site_id, site_code, url_pattern, http_method, access_type,
               required_roles, required_auths, allowed_user_types, allowed_ips,
               require_csrf_yn, require_2fa_yn, priority, description, use_yn
        FROM tb_role_url_access
        WHERE url_access_id = #{urlAccessId} AND delete_yn = 'N'
    </select>

    <select id="countByPattern" resultType="int">
        SELECT COUNT(*) FROM tb_role_url_access
        WHERE url_pattern = #{urlPattern}
          AND http_method = #{httpMethod}
          AND delete_yn = 'N'
        <choose>
            <when test="siteId != null">AND site_id = #{siteId}</when>
            <otherwise>AND site_id IS NULL</otherwise>
        </choose>
        <if test="excludeId != null">AND url_access_id &lt;&gt; #{excludeId}</if>
    </select>

    <insert id="insert" parameterType="com.gonet.primary.auth.dto.UrlAccessRule">
        INSERT INTO tb_role_url_access
            (url_access_id, site_id, site_code, url_pattern, http_method, access_type,
             required_roles, required_auths, allowed_user_types, allowed_ips,
             require_csrf_yn, require_2fa_yn, priority, description, use_yn)
        VALUES
            (#{urlAccessId}, #{siteId}, #{siteCode}, #{urlPattern}, #{httpMethod}, #{accessType},
             #{requiredRoles}, #{requiredAuths}, #{allowedUserTypes}, #{allowedIps},
             #{requireCsrfYn}, #{require2faYn}, #{priority}, #{description}, #{useYn})
    </insert>

    <update id="update" parameterType="com.gonet.primary.auth.dto.UrlAccessRule">
        UPDATE tb_role_url_access
        SET
            <if test="urlPattern != null and urlPattern != ''">url_pattern = #{urlPattern},</if>
            <if test="accessType != null and accessType != ''">access_type = #{accessType},</if>
            <!-- NOT NULL DEFAULT 'ALL' — 빈 값이 오면 기존 값을 지킨다(예전엔 여기서 500 이 났다) -->
            <if test="httpMethod != null and httpMethod != ''">http_method = #{httpMethod},</if>
            <if test="requireCsrfYn != null and requireCsrfYn != ''">require_csrf_yn = #{requireCsrfYn},</if>
            <if test="require2faYn != null and require2faYn != ''">require_2fa_yn = #{require2faYn},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="description != null and description != ''">description = #{description},</if>
            <if test="useYn != null and useYn != ''">use_yn = #{useYn},</if>
            <!--
              조건 컬럼은 전부 무조건 세팅한다. access_type 을 ROLE→PERMIT_ALL 로 바꾸면
              required_roles 등을 <b>반드시 비워야</b> 하는데, <if> 를 걸면 옛 조건이
              남아 인가 판정이 조용히 어긋난다(무매칭 DENY 원칙이 무의미해진다).
              site_id NULL = 전역 규칙이라 이것도 비울 수 있어야 한다.
            -->
            site_id = #{siteId}, site_code = #{siteCode},
            required_roles = #{requiredRoles},
            required_auths = #{requiredAuths},
            allowed_user_types = #{allowedUserTypes},
            allowed_ips = #{allowedIps},
            updated_by = #{updatedBy}, updated_ip = #{updatedIp}, updated_at = CURRENT_TIMESTAMP
        WHERE url_access_id = #{urlAccessId}
    </update>

    <update id="softDelete">
        UPDATE tb_role_url_access
        SET delete_yn = 'Y', use_yn = 'N',
            updated_by = #{updatedBy}, updated_ip = #{updatedIp}, updated_at = CURRENT_TIMESTAMP
        WHERE url_access_id = #{urlAccessId}
    </update>

    <select id="findAuthIdsByRoleIds" resultType="string">
        SELECT DISTINCT ra.auth_id
        FROM tb_role_auth ra
        JOIN tb_auth a ON a.auth_id = ra.auth_id AND a.use_yn = 'Y' AND a.delete_yn = 'N'
        WHERE ra.delete_yn = 'N'
          AND ra.role_id IN
        <foreach item="roleId" collection="roleIds" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </select>

</mapper>
