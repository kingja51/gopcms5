<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gonet.primary.auth.mapper.AuthMapper">

    <!-- 통합 로그인 조회 — user_type 필수 + MEMBER 는 site 스코프 (동일 login_id 양측 공존 가능) -->
    <select id="findLoginUser" resultType="com.gonet.primary.auth.dto.LoginUser">
        SELECT *
        FROM vw_user_login
        WHERE user_type = #{userType}
          AND login_id = #{loginId}
        <if test="siteId != null">
          AND site_id = #{siteId}
        </if>
        <if test="siteId == null">
          AND site_id IS NULL
        </if>
    </select>

    <!-- user_id 는 전역 유일(ADM_/MBR_ 접두 + UUIDv7)이라 사이트 스코프가 필요 없다 -->
    <select id="findLoginUserById" resultType="com.gonet.primary.auth.dto.LoginUser">
        SELECT *
        FROM vw_user_login
        WHERE user_type = #{userType}
          AND user_id = #{userId}
    </select>

    <sql id="activeAllowIp">
          AND use_yn = 'Y'
          AND delete_yn = 'N'
          AND (expires_at IS NULL OR expires_at &gt; NOW())
    </sql>

    <sql id="allowIpColumns">
        ip_id, admin_id, ip_address, ip_type, ip_start, ip_end, description
    </sql>

    <!--
      CIDR/RANGE 는 SQL 로 판정할 수 없어(문자열 비교로는 10.0.0.0/8 을 못 푼다)
      활성 행을 가져와 IpMatch 에서 매칭한다 — /adm/login 폼 노출 게이트 (정책 2026-07-28).
    -->
    <select id="findActiveAllowIps" resultType="com.gonet.primary.auth.dto.AdminAllowIp">
        SELECT <include refid="allowIpColumns"/>
        FROM tb_admin_allow_ip
        WHERE 1 = 1
        <include refid="activeAllowIp"/>
    </select>

    <!-- 인증 조건 — (admin_id, ip) 쌍이 참이어야 로그인 가능 -->
    <select id="findActiveAllowIpsByAdmin" resultType="com.gonet.primary.auth.dto.AdminAllowIp">
        SELECT <include refid="allowIpColumns"/>
        FROM tb_admin_allow_ip
        WHERE admin_id = #{adminId}
        <include refid="activeAllowIp"/>
    </select>

    <!--
      로그인 상태 갱신문의 updated_by 는 본인(userId) 이다 — 로그인 시점에는 SecurityContext
      가 아직 서지 않아 AuditorContext 가 비어 있고, 실제 행위자도 본인이기 때문이다.
      loginFail 은 clientIp 파라미터가 없어 updated_ip 를 건드리지 않는다
      (접속 IP 는 tb_login_history 가 이미 남긴다).
    -->
    <update id="touchAllowIp">
        UPDATE tb_admin_allow_ip
        SET access_count = access_count + 1,
            last_access_at = NOW(),
            updated_at = CURRENT_TIMESTAMP
        WHERE ip_id = #{ipId}
    </update>

    <select id="findTwoFactorRequired" resultType="string">
        SELECT two_factor_required
        FROM tb_admin_group
        WHERE admin_group_id = #{groupId}
          AND use_yn = 'Y'
          AND delete_yn = 'N'
    </select>

    <update id="enableTwoFactor">
        UPDATE tb_admin
        SET two_factor_secret = #{secret},
            two_factor_enabled_yn = 'Y',
            updated_by = #{adminId}, updated_at = CURRENT_TIMESTAMP
        WHERE admin_id = #{adminId}
    </update>

    <!-- 실패: 5회 도달 시 30분 잠금 — 파생 컬럼을 먼저 계산(+1 기준) 후 카운트 증가 -->
    <update id="memberLoginFail">
        UPDATE tb_member
        SET locked_until = IF(login_fail_count + 1 &gt;= 5, DATE_ADD(NOW(), INTERVAL 30 MINUTE), locked_until),
            status = IF(login_fail_count + 1 &gt;= 5, 'LOCKED', status),
            captcha_required_yn = IF(login_fail_count + 1 &gt;= 5, 'Y', captcha_required_yn),
            login_fail_count = login_fail_count + 1,
            updated_by = #{userId}, updated_at = CURRENT_TIMESTAMP
        WHERE member_id = #{userId}
    </update>

    <update id="adminLoginFail">
        UPDATE tb_admin
        SET locked_until = IF(login_fail_count + 1 &gt;= 5, DATE_ADD(NOW(), INTERVAL 30 MINUTE), locked_until),
            status = IF(login_fail_count + 1 &gt;= 5, 'LOCKED', status),
            captcha_required_yn = IF(login_fail_count + 1 &gt;= 5, 'Y', captcha_required_yn),
            login_fail_count = login_fail_count + 1,
            updated_by = #{userId}, updated_at = CURRENT_TIMESTAMP
        WHERE admin_id = #{userId}
    </update>

    <!-- 성공: 실패 카운트·잠금 해제 + 최종 로그인 기록 (잠금 기간 경과 후 성공 시 ACTIVE 복귀) -->
    <update id="memberLoginSuccess">
        UPDATE tb_member
        SET login_fail_count = 0,
            locked_until = NULL,
            captcha_required_yn = 'N',
            status = IF(status = 'LOCKED', 'ACTIVE', status),
            last_login_at = NOW(),
            last_login_ip = #{clientIp},
            last_access_at = NOW(),
            updated_by = #{userId}, updated_ip = #{clientIp}, updated_at = CURRENT_TIMESTAMP
        WHERE member_id = #{userId}
    </update>

    <update id="adminLoginSuccess">
        UPDATE tb_admin
        SET login_fail_count = 0,
            locked_until = NULL,
            captcha_required_yn = 'N',
            status = IF(status = 'LOCKED', 'ACTIVE', status),
            last_login_at = NOW(),
            last_login_ip = #{clientIp},
            last_access_at = NOW(),
            updated_by = #{userId}, updated_ip = #{clientIp}, updated_at = CURRENT_TIMESTAMP
        WHERE admin_id = #{userId}
    </update>

</mapper>
